'use strict';

import SearchGeneral from '../search-general';

describe('SearchGeneral View', function() {

  beforeEach(() => {
    this.searchGeneral = new SearchGeneral();
  });

  it('Should run a few assertions', () => {
    expect(this.searchGeneral);
  });

});
